# 🚜 ระบบขอใช้รถ Forklift

ระบบอนุมัติการใช้รถ Forklift แบบ 2 ขั้นตอน (Manager → Stock) เชื่อมต่อกับ LINE

## วิธีการรัน

1. **คัดลอกไฟล์ Environment Variables:**
   ```bash
   copy .env.local.example .env.local
   ```

2. **ติดตั้ง Dependencies:**
   ```bash
   npm install
   ```

3. **รันโปรเจกต์:**
   ```bash
   npm run dev
   ```

4. **เปิดเบราว์เซอร์:**
   - หน้าฟอร์ม: http://localhost:3000
   - Dashboard: http://localhost:3000/dashboard

## ฟีเจอร์

- ✅ ฟอร์มขอรถ (ชื่อ, แผนก, วัตถุประสงค์, เวลา)
- 📸 ถ่ายรูปสภาพรถก่อนใช้งาน
- 📊 Dashboard สำหรับฝ่าย Stock ดูรายการ
- 🔔 เชื่อมต่อ Supabase สำหรับ Backend & Database

## Supabase Setup

ตั้งค่า Environment Variables ใน `.env.local`:
- `NEXT_PUBLIC_SUPABASE_URL`: https://caysnmdwyiqvavolkclw.supabase.co
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`: [คีย์ที่ได้จาก Supabase]
